---
import { getCollection } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard.astro";
import Link from "@components/Link.astro";
import { dateRange } from "@lib/utils";
import { SITE, HOME, SOCIALS } from "@consts";

const projects = (await getCollection("projects"))
  .filter(project => !project.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_PROJECTS_ON_HOMEPAGE);

const allwork = (await getCollection("work"))
  .sort((a, b) => new Date(b.data.dateStart).valueOf() - new Date(a.data.dateStart).valueOf())
  .slice(0,SITE.NUM_WORKS_ON_HOMEPAGE);

const work = await Promise.all(
  allwork.map(async (item) => {
    const { Content } = await item.render();
    return { ...item, Content };
  })
);

---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <h4 class="animate font-semibold text-black dark:text-white">
      Hi, I'm {SITE.NAME} <span class="text-xl">👋🏻</span>
    </h4>
    <div class="space-y-16">
      <section>
        <article class="space-y-4">
          <p class="animate">
            Mobil ve web uygulamaları geliştirme konusunda deneyimli bir yazılım geliştiricisiyim. Flutter, React ve React Native kullanarak yüksek performanslı ve kullanıcı odaklı çözümler üretiyorum. DIGITURK’te hem front-end hem back-end geliştirme süreçlerinde, API ve Firebase entegrasyonlarıyla ölçeklenebilir ve modern uygulamalar oluşturuyorum.
          </p>
          <p class="animate text-sm opacity-75">
            Software developer with experience in building mobile and web applications. Creates high-performance, user-focused solutions using Flutter, React, and React Native. At DIGITURK, contributes to both front-end and back-end development, building scalable and modern applications with API and Firebase integrations.
          </p>
        </article>
      </section>

      {work.length > 0 &&
        <section class="animate space-y-6">
          <div class="flex flex-wrap gap-y-2 items-center justify-between">
            <h5 class="font-semibold text-black dark:text-white">
              İş deneyimi
            </h5>
            <Link href="/work">
              Tüm deneyimler
            </Link>
          </div>
          <ul class="flex flex-col space-y-4">
            {work.map(entry => (
              <li>
                <div class="text-sm opacity-75">
                  {dateRange(entry.data.dateStart, entry.data.dateEnd)}
                </div>
                <div class="font-semibold text-black dark:text-white">
                  {entry.data.company}
                </div>
                <div class="text-sm opacity-75">
                  {entry.data.role}
                </div>
                <article>
                  <entry.Content />
                </article>
              </li>
            ))}
          </ul>
        </section>
      }

      {projects.length > 0 &&
        <section class="animate space-y-6">
          <div class="flex flex-wrap gap-y-2 items-center justify-between">
            <h5 class="font-semibold text-black dark:text-white">
              Son projeler
            </h5>
            <Link href="/projects">
              Tüm projeler
            </Link>
          </div>
          <ul class="flex flex-col gap-4">
            {projects.map(project => (
              <li>
                <ArrowCard entry={project} />
              </li>
            ))}
          </ul>
        </section>
      }

      <section class="animate space-y-4">
        <h5 class="font-semibold text-black dark:text-white">
          İletişim
        </h5>
        <article>
          <p>
            Eğer projeler, iş fırsatları ya da sadece selam vermek için iletişime geçmek istersen; sosyal medyadan, e-posta veya telefon üzerinden ulaşabilirsin.
          </p>
        </article>
        <ul class="flex flex-wrap gap-2">
          {SOCIALS.map(SOCIAL => (
            <li class="flex gap-x-2 text-nowrap">
              <Link href={SOCIAL.HREF} external aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}>
                {SOCIAL.NAME}
              </Link>
              {"/"}
            </li>
          ))}
          <li class="line-clamp-1">
            <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.NAME}`}>
              {SITE.EMAIL}
            </Link>
          </li>
          <li class="line-clamp-1">
            <a href="tel:+905428030311" aria-label={`Phone ${SITE.NAME}`} class="underline">
              0 542 803 03 11
            </a>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>
